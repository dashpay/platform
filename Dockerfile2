# syntax = docker/dockerfile:1.5

FROM peakcom/s5cmd:v2.0.0
RUN apk add tree
RUN --mount=type=cache,sharing=shared,id=deps_target,target=/deps \
    --mount=type=cache,sharing=shared,id=drive_target,target=/drive \
    tree -L 3 /deps && \
    tree -L 3 /drive
