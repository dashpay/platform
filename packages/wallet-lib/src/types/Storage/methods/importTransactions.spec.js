const { expect } = require('chai');
const importTransactions = require('./importTransactions');

describe('Storage - importTransactions', () => {
  it('should import an array of transaction', () => {

  });
  it('should import an object with one or multiples transaction', () => {

  });
});
