const { expect } = require('chai');
const addUTXOToAddress = require('../../../../src/types/Storage/methods/addUTXOToAddress');

describe('Storage - addUTXOToAddress', () => {
  it('should add a new UTXO to an address', () => {
    const self = {};
  });
});
