const { expect } = require('chai');
const importSingleAddress = require('../../src/Storage/importSingleAddress');

describe('Storage - importSingleAddress', () => {
  it('should import a SingleAddress (non-bip44)', () => {

  });
});
