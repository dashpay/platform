const { expect } = require('chai');
const importAddresses = require('../../src/Storage/importAddresses');

describe('Storage - importAddresses', () => {
  it('should import an array of addresses', () => {
  });
});
