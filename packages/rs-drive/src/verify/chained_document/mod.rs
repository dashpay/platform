//! Chained document query proof verification — the verifier half of the
//! provable semi-join in
//! [`chained_document_query`](crate::query::chained_document_query).

mod verify_chained_documents_proof;
