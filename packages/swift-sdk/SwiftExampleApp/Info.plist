<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- Bundle metadata — substituted from build settings at copy time. -->
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundleDisplayName</key>
	<string>Dash Developer Pro</string>
	<key>CFBundlePackageType</key>
	<string>$(PRODUCT_BUNDLE_PACKAGE_TYPE)</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>LSRequiresIPhoneOS</key>
	<true/>

	<!-- Camera permission — the Send screen's QR scanner reads Dash
	     address QR codes. Without this key the app crashes the moment
	     it touches the camera. -->
	<key>NSCameraUsageDescription</key>
	<string>The camera is used to scan Dash address QR codes.</string>

	<!-- Invitation links use the AppsFlyer applink form
	     (https://invitations.dashpay.io/applink?…) exclusively; the interim
	     dashpay:// custom scheme is retired. Opening links from the OS needs
	     Universal Links: the associated-domains entitlement here plus the
	     domain hosting apple-app-site-association (infra checklist:
	     dashpay/platform#4212). Until then links are claimed via paste/QR. -->

	<!-- UIKit / Scene config (auto-generated equivalents). -->
	<key>UIApplicationSceneManifest</key>
	<dict>
		<key>UIApplicationSupportsMultipleScenes</key>
		<true/>
	</dict>
	<key>UIApplicationSupportsIndirectInputEvents</key>
	<true/>
	<key>UILaunchScreen</key>
	<dict/>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UISupportedInterfaceOrientations~ipad</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationPortraitUpsideDown</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>

	<!--
		⚠️  TEST-ONLY EXCEPTION — REMOVE BEFORE SHIPPING ⚠️

		`NSAllowsArbitraryLoads` lets the app fetch from plain `http://`
		endpoints. Required so `SDK.discoverActiveMasternodes` can hit
		`http://<host>:8080/masternodes` (the dash quorum-list-server
		runs HTTP by default — see
		https://github.com/dashpay/quorum-list-server).

		On a production build this MUST be removed and the
		quorum-list-server fronted by HTTPS. Tracked alongside the rest
		of the devnet-test scaffolding (dashpay/platform#3714).
	-->
	<key>NSAppTransportSecurity</key>
	<dict>
		<key>NSAllowsArbitraryLoads</key>
		<true/>
	</dict>
</dict>
</plist>
