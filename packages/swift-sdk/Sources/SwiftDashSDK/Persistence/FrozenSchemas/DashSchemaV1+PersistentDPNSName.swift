import Foundation
import SwiftData

// `PersistentDPNSName` exactly as schema DashSchemaV1 registered it, generated by
// scripts/freeze_schema_models.py from the live model at commit 96a103375e.
// Do not edit: every stored property, its optionality and default, and
// every @Attribute / @Relationship / #Index / #Unique here is an input to
// that version's checksum, and changing any of them re-breaks the stores
// this copy exists to keep openable. See the live model for what each
// column means.
extension DashSchemaV1 {
    @Model
    final class PersistentDPNSName {
        #Unique<PersistentDPNSName>([\.networkRaw, \.normalizedParentDomainName, \.normalizedLabel])

        var networkRaw: UInt32

        var network: Network {
            get { Network(rawValue: networkRaw) ?? .testnet }
            set { networkRaw = newValue.rawValue }
        }

        var label: String

        var normalizedLabel: String

        var parentDomainName: String

        var normalizedParentDomainName: String

        var acquiredAt: UInt64

        var isOwned: Bool = true

        var documentIdBase58: String?

        var priceCredits: Int64?

        var saleStatusRaw: Int16 = 0

        var counterpartyIdBase58: String?

        var documentCreatedAtMs: UInt64?

        var documentUpdatedAtMs: UInt64?

        var documentTransferredAtMs: UInt64?

        var marketplaceUpdatedAt: UInt64 = 0

        var identity: PersistentIdentity

        var createdAt: Date
        var lastUpdated: Date

        init(
            identity: PersistentIdentity,
            label: String,
            parentDomainName: String = "dash",
            acquiredAt: UInt64 = 0,
            isOwned: Bool = true
        ) {
            self.identity = identity
            self.networkRaw = identity.networkRaw
            self.label = label
            self.normalizedLabel = Self.normalize(label)
            self.parentDomainName = parentDomainName
            self.normalizedParentDomainName = Self.normalize(parentDomainName)
            self.acquiredAt = acquiredAt
            self.isOwned = isOwned
            self.documentIdBase58 = nil
            self.priceCredits = nil
            self.saleStatusRaw = 0
            self.counterpartyIdBase58 = nil
            self.documentCreatedAtMs = nil
            self.documentUpdatedAtMs = nil
            self.documentTransferredAtMs = nil
            self.marketplaceUpdatedAt = 0
            self.createdAt = Date()
            self.lastUpdated = Date()
        }
    }
}

extension DashSchemaV1.PersistentDPNSName {
    var saleStatus: DpnsNameSaleStatus? {
        guard documentIdBase58 != nil else { return nil }
        switch saleStatusRaw {
        case 0:
            return .owned
        case 1:
            guard let to = counterpartyId else { return nil }
            return .sold(to: to)
        case 2:
            guard let to = counterpartyId else { return nil }
            return .transferred(to: to)
        default:
            return nil
        }
    }

    var counterpartyId: Data? {
        counterpartyIdBase58.flatMap { Data.identifier(fromBase58: $0) }
    }

    var listedPriceCredits: UInt64? {
        guard documentIdBase58 != nil, let priceCredits else { return nil }
        return UInt64(bitPattern: priceCredits)
    }
}

extension DashSchemaV1.PersistentDPNSName {
    static func normalize(_ input: String) -> String {
        String(input.map { c -> Character in
            switch c {
            case "o", "O": return "0"
            case "i", "I": return "1"
            case "l", "L": return "1"
            default: return Character(c.lowercased())
            }
        })
    }
}

extension DashSchemaV1.PersistentDPNSName {
    static func predicate(identityId: Data) -> Predicate<DashSchemaV1.PersistentDPNSName> {
        let target = identityId
        return #Predicate<DashSchemaV1.PersistentDPNSName> { name in
            name.identity.identityId == target && name.isOwned == true
        }
    }
}
