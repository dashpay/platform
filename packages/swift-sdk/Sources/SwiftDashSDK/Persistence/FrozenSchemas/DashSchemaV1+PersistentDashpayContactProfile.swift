import Foundation
import SwiftData

// `PersistentDashpayContactProfile` exactly as schema DashSchemaV1 registered it, generated by
// scripts/freeze_schema_models.py from the live model at commit 96a103375e.
// Do not edit: every stored property, its optionality and default, and
// every @Attribute / @Relationship / #Index / #Unique here is an input to
// that version's checksum, and changing any of them re-breaks the stores
// this copy exists to keep openable. See the live model for what each
// column means.
extension DashSchemaV1 {
    @Model
    final class PersistentDashpayContactProfile {
        #Unique<PersistentDashpayContactProfile>([
            \.networkRaw, \.ownerIdentityId, \.contactIdentityId
        ])

        var networkRaw: UInt32

        var network: Network {
            get { Network(rawValue: networkRaw) ?? .testnet }
            set { networkRaw = newValue.rawValue }
        }

        var ownerIdentityId: Data

        var contactIdentityId: Data

        var displayName: String?

        var publicMessage: String?

        var bio: String?

        var avatarUrl: String?

        var avatarHash: Data?

        var avatarFingerprint: Data?

        var checkedAtMs: UInt64

        var owner: PersistentIdentity

        var createdAt: Date
        var lastUpdated: Date

        init(
            owner: PersistentIdentity,
            contactIdentityId: Data,
            checkedAtMs: UInt64,
            displayName: String? = nil,
            publicMessage: String? = nil,
            bio: String? = nil,
            avatarUrl: String? = nil,
            avatarHash: Data? = nil,
            avatarFingerprint: Data? = nil
        ) {
            self.owner = owner
            self.networkRaw = owner.networkRaw
            self.ownerIdentityId = owner.identityId
            self.contactIdentityId = contactIdentityId
            self.checkedAtMs = checkedAtMs
            self.displayName = displayName
            self.publicMessage = publicMessage
            self.bio = bio
            self.avatarUrl = avatarUrl
            self.avatarHash = avatarHash
            self.avatarFingerprint = avatarFingerprint
            self.createdAt = Date()
            self.lastUpdated = Date()
        }
    }
}

extension DashSchemaV1.PersistentDashpayContactProfile {
    static func predicate(
        ownerIdentityId: Data
    ) -> Predicate<DashSchemaV1.PersistentDashpayContactProfile> {
        let target = ownerIdentityId
        return #Predicate<DashSchemaV1.PersistentDashpayContactProfile> { row in
            row.ownerIdentityId == target
        }
    }

    static func predicate(
        ownerIdentityId: Data,
        contactIdentityId: Data
    ) -> Predicate<DashSchemaV1.PersistentDashpayContactProfile> {
        let target = ownerIdentityId
        let contact = contactIdentityId
        return #Predicate<DashSchemaV1.PersistentDashpayContactProfile> { row in
            row.ownerIdentityId == target
                && row.contactIdentityId == contact
        }
    }
}
