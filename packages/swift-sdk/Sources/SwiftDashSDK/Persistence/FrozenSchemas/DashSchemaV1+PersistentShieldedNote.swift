import Foundation
import SwiftData

// `PersistentShieldedNote` exactly as schema DashSchemaV1 registered it, generated by
// scripts/freeze_schema_models.py from the live model at commit 96a103375e.
// Do not edit: every stored property, its optionality and default, and
// every @Attribute / @Relationship / #Index / #Unique here is an input to
// that version's checksum, and changing any of them re-breaks the stores
// this copy exists to keep openable. See the live model for what each
// column means.
extension DashSchemaV1 {
    @Model
    final class PersistentShieldedNote {
        #Index<PersistentShieldedNote>([\.walletId, \.accountIndex])

        var walletId: Data
        var accountIndex: UInt32
        var position: UInt64
        var cmx: Data
        @Attribute(.unique) var nullifier: Data
        var blockHeight: UInt64
        var isSpent: Bool
        var value: UInt64
        var noteData: Data

        var createdAt: Date
        var lastUpdated: Date

        init(
            walletId: Data,
            accountIndex: UInt32,
            position: UInt64,
            cmx: Data,
            nullifier: Data,
            blockHeight: UInt64,
            isSpent: Bool,
            value: UInt64,
            noteData: Data
        ) {
            self.walletId = walletId
            self.accountIndex = accountIndex
            self.position = position
            self.cmx = cmx
            self.nullifier = nullifier
            self.blockHeight = blockHeight
            self.isSpent = isSpent
            self.value = value
            self.noteData = noteData
            let now = Date()
            self.createdAt = now
            self.lastUpdated = now
        }
    }
}

extension DashSchemaV1.PersistentShieldedNote {
    static func unspentPredicate(walletId: Data) -> Predicate<DashSchemaV1.PersistentShieldedNote> {
        #Predicate<DashSchemaV1.PersistentShieldedNote> {
            $0.walletId == walletId && $0.isSpent == false
        }
    }

    static var unspentPredicate: Predicate<DashSchemaV1.PersistentShieldedNote> {
        #Predicate<DashSchemaV1.PersistentShieldedNote> { $0.isSpent == false }
    }
}
