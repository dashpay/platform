import Foundation
import SwiftData

// `PersistentKeyword` exactly as schema DashSchemaV4 registered it, generated by
// scripts/freeze_schema_models.py from the live model at commit d8b7944ef3.
// Do not edit: every stored property, its optionality and default, and
// every @Attribute / @Relationship / #Unique here is an input to that
// version's checksum, and every #Index to the store's SQLite indexes;
// changing any of them re-breaks the stores this copy exists to keep
// openable. See the live model for what each column means.
extension DashSchemaV4 {
    @Model
    final class PersistentKeyword {
        @Attribute(.unique) var id: String
        var keyword: String
        var contractId: String

        var dataContract: PersistentDataContract?

        init(keyword: String, contractId: String) {
            self.id = "\(contractId)_\(keyword)"
            self.keyword = keyword
            self.contractId = contractId
        }
    }
}

extension DashSchemaV4.PersistentKeyword {
    static func predicate(keyword: String) -> Predicate<DashSchemaV4.PersistentKeyword> {
        #Predicate<DashSchemaV4.PersistentKeyword> { item in
            item.keyword.localizedStandardContains(keyword)
        }
    }

    static func predicate(contractId: String) -> Predicate<DashSchemaV4.PersistentKeyword> {
        #Predicate<DashSchemaV4.PersistentKeyword> { item in
            item.contractId == contractId
        }
    }
}
