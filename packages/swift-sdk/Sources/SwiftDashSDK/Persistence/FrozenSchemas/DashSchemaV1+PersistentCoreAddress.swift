import Foundation
import SwiftData

// `PersistentCoreAddress` exactly as schema DashSchemaV1 registered it, generated by
// scripts/freeze_schema_models.py from the live model at commit 5f58417079.
// Do not edit: every stored property, its optionality and default, and
// every @Attribute / @Relationship / #Index / #Unique here is an input to
// that version's checksum, and changing any of them re-breaks the stores
// this copy exists to keep openable. See the live model for what each
// column means.
extension DashSchemaV1 {
    @Model
    final class PersistentCoreAddress {
        @Attribute(.unique) var address: String
        var publicKey: Data
        var keyType: UInt8 = 0
        var poolTypeTag: UInt8
        var addressIndex: UInt32
        var derivationPath: String
        var isUsed: Bool
        var firstSeenHeight: UInt32
        var lastSeenHeight: UInt32
        var balance: UInt64
        var createdAt: Date
        var lastUpdated: Date

        var account: PersistentAccount?

        @Relationship(deleteRule: .cascade, inverse: \PersistentTxo.coreAddress)
        var txos: [PersistentTxo] = []

        init(
            address: String,
            publicKey: Data = Data(),
            keyType: UInt8 = 0,
            poolTypeTag: UInt8,
            addressIndex: UInt32,
            derivationPath: String,
            isUsed: Bool = false,
            balance: UInt64 = 0
        ) {
            self.address = address
            self.publicKey = publicKey
            self.keyType = keyType
            self.poolTypeTag = poolTypeTag
            self.addressIndex = addressIndex
            self.derivationPath = derivationPath
            self.isUsed = isUsed
            self.firstSeenHeight = 0
            self.lastSeenHeight = 0
            self.balance = balance
            self.createdAt = Date()
            self.lastUpdated = Date()
        }
    }
}

extension DashSchemaV1.PersistentCoreAddress {
    var poolTypeName: String {
        switch poolTypeTag {
        case 0: return "External"
        case 1: return "Internal"
        case 2: return "Additional"
        case 3: return "Additional (Hardened)"
        default: return "Unknown(\(poolTypeTag))"
        }
    }
}
