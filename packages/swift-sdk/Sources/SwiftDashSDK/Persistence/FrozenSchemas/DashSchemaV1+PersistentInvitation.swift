import Foundation
import SwiftData

// `PersistentInvitation` exactly as schema DashSchemaV1 registered it, generated by
// scripts/freeze_schema_models.py from the live model at commit 96a103375e.
// Do not edit: every stored property, its optionality and default, and
// every @Attribute / @Relationship / #Index / #Unique here is an input to
// that version's checksum, and changing any of them re-breaks the stores
// this copy exists to keep openable. See the live model for what each
// column means.
extension DashSchemaV1 {
    @Model
    final class PersistentInvitation {
        #Index<PersistentInvitation>([\.walletId])

        @Attribute(.unique) var outPointHex: String

        var rawOutPoint: Data

        var walletId: Data

        var fundingIndexRaw: Int

        var amountDuffs: Int64

        var expiryUnix: Int

        var createdAtSecs: Int

        var hasInviter: Bool

        var statusRaw: Int

        var reclaimInFlight: Bool = false

        var createdAt: Date
        var updatedAt: Date

        init(
            outPointHex: String,
            rawOutPoint: Data,
            walletId: Data,
            fundingIndexRaw: Int,
            amountDuffs: Int64,
            expiryUnix: Int,
            createdAtSecs: Int,
            hasInviter: Bool,
            statusRaw: Int,
            reclaimInFlight: Bool = false
        ) {
            self.outPointHex = outPointHex
            self.rawOutPoint = rawOutPoint
            self.walletId = walletId
            self.fundingIndexRaw = fundingIndexRaw
            self.amountDuffs = amountDuffs
            self.expiryUnix = expiryUnix
            self.createdAtSecs = createdAtSecs
            self.hasInviter = hasInviter
            self.statusRaw = statusRaw
            self.reclaimInFlight = reclaimInFlight
            self.createdAt = Date()
            self.updatedAt = Date()
        }
    }
}

extension DashSchemaV1.PersistentInvitation {
    static func predicate(walletId: Data) -> Predicate<DashSchemaV1.PersistentInvitation> {
        #Predicate<DashSchemaV1.PersistentInvitation> { entry in
            entry.walletId == walletId
        }
    }
}
