import Foundation
import SwiftData

// `PersistentShieldedActivity` exactly as schema DashSchemaV1 registered it, generated by
// scripts/freeze_schema_models.py from the live model at commit 96a103375e.
// Do not edit: every stored property, its optionality and default, and
// every @Attribute / @Relationship / #Index / #Unique here is an input to
// that version's checksum, and changing any of them re-breaks the stores
// this copy exists to keep openable. See the live model for what each
// column means.
extension DashSchemaV1 {
    @Model
    final class PersistentShieldedActivity {
        #Unique<PersistentShieldedActivity>([\.walletId, \.accountIndex, \.entryId])
        #Index<PersistentShieldedActivity>([\.walletId, \.accountIndex])

        var walletId: Data
        var accountIndex: UInt32
        var entryId: Data

        var kindTag: Int
        var direction: Int
        var status: Int

        var amount: UInt64
        var fee: UInt64
        var hasFee: Bool
        var blockHeight: UInt64
        var hasBlockHeight: Bool
        var createdAtMs: UInt64

        var minNotePosition: UInt64 = 0
        var hasMinNotePosition: Bool = false

        var identityId: Data
        var counterparty: Data
        var memo: Data
        var noteCmxs: Data
        var spentNullifiers: Data

        var createdAt: Date
        var lastUpdated: Date

        init(
            walletId: Data,
            accountIndex: UInt32,
            entryId: Data,
            kindTag: Int,
            direction: Int,
            status: Int,
            amount: UInt64,
            fee: UInt64,
            hasFee: Bool,
            blockHeight: UInt64,
            hasBlockHeight: Bool,
            createdAtMs: UInt64,
            minNotePosition: UInt64 = 0,
            hasMinNotePosition: Bool = false,
            identityId: Data,
            counterparty: Data,
            memo: Data,
            noteCmxs: Data,
            spentNullifiers: Data
        ) {
            self.walletId = walletId
            self.accountIndex = accountIndex
            self.entryId = entryId
            self.kindTag = kindTag
            self.direction = direction
            self.status = status
            self.amount = amount
            self.fee = fee
            self.hasFee = hasFee
            self.blockHeight = blockHeight
            self.hasBlockHeight = hasBlockHeight
            self.createdAtMs = createdAtMs
            self.minNotePosition = minNotePosition
            self.hasMinNotePosition = hasMinNotePosition
            self.identityId = identityId
            self.counterparty = counterparty
            self.memo = memo
            self.noteCmxs = noteCmxs
            self.spentNullifiers = spentNullifiers
            let now = Date()
            self.createdAt = now
            self.lastUpdated = now
        }
    }
}
