import Foundation
import SwiftData

// `PersistentShieldedOutgoingNote` exactly as schema DashSchemaV1 registered it, generated by
// scripts/freeze_schema_models.py from the live model at commit 5f58417079.
// Do not edit: every stored property, its optionality and default, and
// every @Attribute / @Relationship / #Index / #Unique here is an input to
// that version's checksum, and changing any of them re-breaks the stores
// this copy exists to keep openable. See the live model for what each
// column means.
extension DashSchemaV1 {
    @Model
    final class PersistentShieldedOutgoingNote {
        #Unique<PersistentShieldedOutgoingNote>([\.walletId, \.accountIndex, \.cmx])
        #Index<PersistentShieldedOutgoingNote>([\.walletId, \.accountIndex])

        var walletId: Data
        var accountIndex: UInt32
        var cmx: Data
        var recipient: Data
        var value: UInt64
        var memo: Data
        var blockHeight: UInt64

        var createdAt: Date
        var lastUpdated: Date

        init(
            walletId: Data,
            accountIndex: UInt32,
            cmx: Data,
            recipient: Data,
            value: UInt64,
            memo: Data,
            blockHeight: UInt64
        ) {
            self.walletId = walletId
            self.accountIndex = accountIndex
            self.cmx = cmx
            self.recipient = recipient
            self.value = value
            self.memo = memo
            self.blockHeight = blockHeight
            let now = Date()
            self.createdAt = now
            self.lastUpdated = now
        }
    }
}
