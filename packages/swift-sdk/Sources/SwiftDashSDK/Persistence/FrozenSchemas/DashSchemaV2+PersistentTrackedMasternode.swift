import Foundation
import SwiftData

// `PersistentTrackedMasternode` exactly as schema DashSchemaV2 registered it, generated by
// scripts/freeze_schema_models.py from the live model at commit 5f58417079.
// Do not edit: every stored property, its optionality and default, and
// every @Attribute / @Relationship / #Index / #Unique here is an input to
// that version's checksum, and changing any of them re-breaks the stores
// this copy exists to keep openable. See the live model for what each
// column means.
extension DashSchemaV2 {
    @Model
    final class PersistentTrackedMasternode {
        #Unique<PersistentTrackedMasternode>([\.networkRaw, \.proTxHash])
        #Index<PersistentTrackedMasternode>([\.networkRaw])

        var networkRaw: UInt32
        var proTxHash: Data
        var label: String?
        var addedAt: UInt64
        var snapshotJSON: String

        var network: Network? {
            get { Network(rawValue: networkRaw) }
            set { networkRaw = newValue?.rawValue ?? networkRaw }
        }

        init(
            networkRaw: UInt32,
            proTxHash: Data,
            label: String?,
            addedAt: UInt64,
            snapshotJSON: String
        ) {
            self.networkRaw = networkRaw
            self.proTxHash = proTxHash
            self.label = label
            self.addedAt = addedAt
            self.snapshotJSON = snapshotJSON
        }
    }
}
