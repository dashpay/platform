import Foundation
import SwiftData

// `PersistentDashpayProfile` exactly as schema DashSchemaV1 registered it, generated by
// scripts/freeze_schema_models.py from the live model at commit 5f58417079.
// Do not edit: every stored property, its optionality and default, and
// every @Attribute / @Relationship / #Index / #Unique here is an input to
// that version's checksum, and changing any of them re-breaks the stores
// this copy exists to keep openable. See the live model for what each
// column means.
extension DashSchemaV1 {
    @Model
    final class PersistentDashpayProfile {
        #Unique<PersistentDashpayProfile>([\.networkRaw, \.identity])

        var networkRaw: UInt32

        var network: Network {
            get { Network(rawValue: networkRaw) ?? .testnet }
            set { networkRaw = newValue.rawValue }
        }

        var displayName: String?

        var publicMessage: String?

        var bio: String?

        var avatarUrl: String?

        var avatarHash: Data?

        var avatarFingerprint: Data?

        var identity: PersistentIdentity

        var createdAt: Date
        var lastUpdated: Date

        init(
            identity: PersistentIdentity,
            displayName: String? = nil,
            publicMessage: String? = nil,
            bio: String? = nil,
            avatarUrl: String? = nil,
            avatarHash: Data? = nil,
            avatarFingerprint: Data? = nil
        ) {
            self.identity = identity
            self.networkRaw = identity.networkRaw
            self.displayName = displayName
            self.publicMessage = publicMessage
            self.bio = bio
            self.avatarUrl = avatarUrl
            self.avatarHash = avatarHash
            self.avatarFingerprint = avatarFingerprint
            self.createdAt = Date()
            self.lastUpdated = Date()
        }
    }
}

extension DashSchemaV1.PersistentDashpayProfile {
    static func predicate(identityId: Data) -> Predicate<DashSchemaV1.PersistentDashpayProfile> {
        let target = identityId
        return #Predicate<DashSchemaV1.PersistentDashpayProfile> { profile in
            profile.identity.identityId == target
        }
    }
}
