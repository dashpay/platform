import Foundation
import SwiftData

// `PersistentPlatformAddress` exactly as schema DashSchemaV1 registered it, generated by
// scripts/freeze_schema_models.py from the live model at commit 5f58417079.
// Do not edit: every stored property, its optionality and default, and
// every @Attribute / @Relationship / #Index / #Unique here is an input to
// that version's checksum, and changing any of them re-breaks the stores
// this copy exists to keep openable. See the live model for what each
// column means.
extension DashSchemaV1 {
    @Model
    final class PersistentPlatformAddress {
        #Index<PersistentPlatformAddress>([\.walletId])

        @Attribute(.unique) var address: String
        var addressType: UInt8
        @Attribute(.unique) var addressHash: Data
        var publicKey: Data
        var accountIndex: UInt32
        var addressIndex: UInt32
        var derivationPath: String
        var isUsed: Bool
        var balance: UInt64
        var nonce: UInt32
        var firstSeenHeight: UInt32
        var lastSeenHeight: UInt64
        var walletId: Data
        var createdAt: Date
        var lastUpdated: Date

        var account: PersistentAccount?

        init(
            address: String,
            addressType: UInt8,
            addressHash: Data,
            publicKey: Data = Data(),
            accountIndex: UInt32,
            addressIndex: UInt32,
            derivationPath: String,
            isUsed: Bool = false,
            balance: UInt64 = 0,
            nonce: UInt32 = 0,
            walletId: Data
        ) {
            self.address = address
            self.addressType = addressType
            self.addressHash = addressHash
            self.publicKey = publicKey
            self.accountIndex = accountIndex
            self.addressIndex = addressIndex
            self.derivationPath = derivationPath
            self.isUsed = isUsed
            self.balance = balance
            self.nonce = nonce
            self.firstSeenHeight = 0
            self.lastSeenHeight = 0
            self.walletId = walletId
            self.createdAt = Date()
            self.lastUpdated = Date()
        }
    }
}

extension DashSchemaV1.PersistentPlatformAddress {
    static func predicate(walletId: Data) -> Predicate<DashSchemaV1.PersistentPlatformAddress> {
        #Predicate<DashSchemaV1.PersistentPlatformAddress> { entry in
            entry.walletId == walletId
        }
    }

    static var nonZeroBalancesPredicate: Predicate<DashSchemaV1.PersistentPlatformAddress> {
        #Predicate<DashSchemaV1.PersistentPlatformAddress> { entry in
            entry.balance > 0
        }
    }
}
