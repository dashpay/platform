import Foundation
import SwiftData

// `PersistentPendingInput` exactly as schema DashSchemaV4 registered it, generated by
// scripts/freeze_schema_models.py from the live model at commit d8b7944ef3.
// Do not edit: every stored property, its optionality and default, and
// every @Attribute / @Relationship / #Unique here is an input to that
// version's checksum, and every #Index to the store's SQLite indexes;
// changing any of them re-breaks the stores this copy exists to keep
// openable. See the live model for what each column means.
extension DashSchemaV4 {
    @Model
    final class PersistentPendingInput {
        #Index<PersistentPendingInput>([\.outpoint], [\.walletId], [\.walletId, \.isSweptTombstone])
        var outpoint: Data

        var inputIndex: UInt32

        var spendingTxid: Data

        var spendingTransaction: PersistentTransaction?

        var walletId: Data

        var createdAt: Date

        var isSweptTombstone: Bool = false

        var winnerMinedHeight: UInt32?

        init(
            outpoint: Data,
            inputIndex: UInt32,
            spendingTxid: Data,
            spendingTransaction: PersistentTransaction?,
            walletId: Data
        ) {
            self.outpoint = outpoint
            self.inputIndex = inputIndex
            self.spendingTxid = spendingTxid
            self.spendingTransaction = spendingTransaction
            self.walletId = walletId
            self.createdAt = Date()
        }
    }
}
