import Foundation
import SwiftData

// `PersistentShieldedSyncState` exactly as schema DashSchemaV1 registered it, generated by
// scripts/freeze_schema_models.py from the live model at commit 96a103375e.
// Do not edit: every stored property, its optionality and default, and
// every @Attribute / @Relationship / #Index / #Unique here is an input to
// that version's checksum, and changing any of them re-breaks the stores
// this copy exists to keep openable. See the live model for what each
// column means.
extension DashSchemaV1 {
    @Model
    final class PersistentShieldedSyncState {
        #Unique<PersistentShieldedSyncState>([\.walletId, \.accountIndex])
        #Index<PersistentShieldedSyncState>([\.walletId])

        var walletId: Data
        var accountIndex: UInt32
        var lastSyncedIndex: UInt64

        var lastUpdated: Date

        init(
            walletId: Data,
            accountIndex: UInt32,
            lastSyncedIndex: UInt64 = 0
        ) {
            self.walletId = walletId
            self.accountIndex = accountIndex
            self.lastSyncedIndex = lastSyncedIndex
            self.lastUpdated = Date()
        }
    }
}
