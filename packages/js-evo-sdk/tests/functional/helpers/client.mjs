import { EvoSDK } from '../../../dist/evo-sdk.module.js';

let client;

export async function getClient(options = {}) {
  if (!client) {
    // Default to testnet with proofs off for speed; adjust per-suite when needed

  }

  return client;
}

