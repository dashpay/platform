version: '3.7'

services:
  dashmate_helper:
    build:
      context: ${DASHMATE_HELPER_DOCKER_BUILD_CONTEXT:?err}
      dockerfile: ${DASHMATE_HELPER_DOCKER_BUILD_DOCKER_FILE_PATH:?err}/packages/dashmate/Dockerfile
    image: dashmate_helper:local
    stop_grace_period: 10s
