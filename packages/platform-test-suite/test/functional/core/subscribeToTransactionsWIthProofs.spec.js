describe('Core', () => {
  describe('subscribeToTransactionsWithProofs', () => {
    it('should respond with only historical data');
    it('should respond with both historical and new data');
    it('should respond with a proper historical and new data in case of reorganization');
    it('should respond with only new data');
  });
});
