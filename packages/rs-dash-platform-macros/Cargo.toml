[package]

name = "dash-platform-macros"
version.workspace = true
edition = "2024"
description = "Proc macros used by Dash Platform. Internal use only."

[lib]
proc-macro = true

[dependencies]
quote = "1.0.37"
syn = { version = "2.0.75", features = ["full"] }
heck = "0.5.0"

[dev-dependencies]

dapi-grpc = { path = "../dapi-grpc" }
