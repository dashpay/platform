[package]
name = "rs-drive-light-client-bindings"
version = "0.1.0"
edition = "2021"
description = "This crate is only used to build bindings (using build.rs) for rs-drive-light-client and save them into bindings/"

[dependencies]
[build-dependencies]

rs-drive-light-client = { path = "../rs-drive-light-client", features = [
    "bindgen",
] }
